# Задание
Посчитать среднее время, за которое пользователи проходят урок по следующему алгоритму:

* для каждого пользователя вычислить время прохождения шага как сумму времени, 
потраченного на каждую попытку (время попытки - это разница между временем отправки задания и временем начала попытки),
при этом попытки, которые длились больше 4 часов не учитывать, так как пользователь мог просто оставить задание
открытым в браузере, а вернуться к нему на следующий день;

* для каждого студента посчитать общее время, которое он затратил на каждый урок;

* вычислить среднее время выполнения урока в часах, результат округлить до 2-х знаков после запятой;

* вывести информацию по возрастанию времени, пронумеровав строки, для каждого урока указать номер модуля
и его позицию в нем.

Результат:


| Номер | Урок                                                        | Среднее_время |
|-------|-------------------------------------------------------------|---------------|
| 1     | 2.2 Запросы на выборку, соединение таблиц                   | 2.37          |
| 2     | 1.2 Выборка данных                                          | 2.65          |
| 3     | 2.4 База данных "Интернет-магазин книг", запросы на выборку | 3.65          |




```sql

-- prepare 1

SELECT module_id, lesson_name, student_id, (submission_time - attempt_time)::numeric
FROM step_student
         NATURAL JOIN lesson


--prepare 2

SELECT lesson_id,
       student_id,
       SUM(CASE
               WHEN (submission_time - attempt_time)::numeric / 3600 <= 4
                   THEN (submission_time - attempt_time)::numeric / 3600
               ELSE 0
           END)
           AS Время
FROM step_student
         NATURAL JOIN step
GROUP BY 1, 2;

--prepare 3

WITH temp AS
         (SELECT lesson_id,
                 student_id,
                 SUM(CASE
                         WHEN (submission_time - attempt_time)::numeric / 3600 <= 4
                             THEN (submission_time - attempt_time)::numeric / 3600
                         ELSE 0
                     END
                 ) AS Время
          FROM step_student
                   NATURAL JOIN step
          GROUP BY student_id, lesson_id)
SELECT *
FROM temp;


--prepare 3

WITH temp AS
         (SELECT lesson_id,
                 student_id,
                 SUM(CASE
                         WHEN (submission_time - attempt_time)::numeric / 3600 <= 4
                             THEN (submission_time - attempt_time)::numeric / 3600
                         ELSE 0
                     END)
                     AS Время
          FROM step_student
                   NATURAL JOIN step
          GROUP BY 1, 2)
SELECT student_id, lesson_name, Время
FROM temp
         NATURAL JOIN lesson;

-- solution-1

WITH temp AS
         (SELECT lesson_id,
                 student_id,
                 SUM(CASE
                         WHEN (submission_time - attempt_time)::numeric / 3600 <= 4
                             THEN (submission_time - attempt_time)::numeric / 3600
                         ELSE 0
                     END)
                     AS Время
          FROM step_student
                   NATURAL JOIN step
          GROUP BY 1, 2)
SELECT ROW_NUMBER() OVER (ORDER BY AVG(Время))                   AS Номер,
       CONCAT(module_id, '.', lesson_position, ' ', lesson_name) AS "Урок",
       ROUND(AVG(Время), 2)                                      AS Среднее_время
FROM temp
         NATURAL JOIN lesson ls
GROUP BY lesson_name, lesson_position, module_id
ORDER BY Среднее_время;


-- solution-2

SELECT ROW_NUMBER() OVER (ORDER BY AVG(Время)) AS Номер,
       CONCAT(module_id, '.', lesson_position, ' ', lesson_name)
                                               AS Урок,
       ROUND(AVG(Время), 2)                    AS Среднее_время
FROM lesson
         NATURAL JOIN (SELECT lesson_id,
                              SUM(CASE
                                      WHEN (submission_time - attempt_time)::numeric / 3600 <= 4
                                          THEN (submission_time - attempt_time)::numeric / 3600
                                      ELSE 0
                                  END)
                                  AS Время
                       FROM step_student
                                NATURAL JOIN step
                       GROUP BY student_id, lesson_id)
GROUP BY Урок;


-- solution-3

SELECT ROW_NUMBER()
       OVER (ORDER BY SUM(submission_time - attempt_time)::numeric / COUNT(DISTINCT student_id))   AS Номер,
       CONCAT(module_id, '.', lesson_position, ' ', lesson_name)                                   AS Урок,
       ROUND(SUM(submission_time - attempt_time) ::numeric / COUNT(DISTINCT student_id) / 3600, 2) AS Среднее_время
FROM step_student
         NATURAL JOIN step
         NATURAL JOIN lesson
WHERE submission_time - attempt_time <= 4 * 3600
GROUP BY Урок;

```

